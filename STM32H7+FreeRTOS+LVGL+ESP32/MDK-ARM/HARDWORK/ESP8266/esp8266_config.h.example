// 本文件为示例模板（可提交到仓库）。
// 使用方法：
// 1) 复制本文件为同目录下的 `esp8266_config.h`
// 2) 在 `esp8266_config.h` 中填写你自己的 WiFi/服务器参数
// 3) 注意：`esp8266_config.h` 已在 .gitignore 中忽略，避免把敏感信息提交到仓库
//
// 典型配置：
// - WIFI_SSID / WIFI_PASSWORD: ESP8266 要连接的路由器 WiFi
// - SERVER_IP / SERVER_PORT: 运行 EdgeWind 后端服务的电脑 IP 与端口
//   例如你电脑是 192.168.10.43，服务器端口 5000，则填：
//     SERVER_IP   "192.168.10.43"
//     SERVER_PORT 5000

#ifndef __ESP8266_CONFIG_H
#define __ESP8266_CONFIG_H

/* ================= 用户配置区（示例） ================= */

// WiFi
#define WIFI_SSID       "YOUR_WIFI_SSID"
#define WIFI_PASSWORD   "YOUR_WIFI_PASSWORD"

// EdgeWind 服务器
#define SERVER_IP       "192.168.10.43"
#define SERVER_PORT     5000

// 节点信息
#define NODE_ID         "STM32_H7_Node"
#define NODE_LOCATION   "Lab_Test"

#endif /* __ESP8266_CONFIG_H */

